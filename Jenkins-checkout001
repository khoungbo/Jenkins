pipeline {
    agent any

    stages {
        stage('Clean Workspace') {
            steps {
                cleanWs()
            }
        }
        stage('Check out SCM') {
            steps {
                sh 'git clone https://github.com/khoungbo/Jenkins.git'
            }
        }
    }
}
